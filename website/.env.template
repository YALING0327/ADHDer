# ===================================
# ADHDER 环境变量配置模板
# ===================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 填入真实的配置信息
# 3. .env 文件已在 .gitignore 中，不会被提交

NODE_ENV=production
PORT=3000
BASE_URL=https://your-api-domain.com

# ===================================
# 数据库配置
# ===================================
# MongoDB Atlas连接字符串
# 获取方式：登录 https://cloud.mongodb.com → 选择集群 → Connect → Connect your application
# 格式示例：mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URL=<YOUR_MONGODB_CONNECTION_STRING_HERE>

# Redis缓存（可选）
REDIS_HOST=<YOUR_REDIS_HOST>
REDIS_PORT=6379
REDIS_PASSWORD=<YOUR_REDIS_PASSWORD>

# ===================================
# 腾讯云服务配置
# ===================================
# 腾讯云API密钥
# 获取方式：腾讯云控制台 → 访问管理 → 访问密钥 → API密钥管理
TENCENT_SECRET_ID=<YOUR_TENCENT_SECRET_ID>
TENCENT_SECRET_KEY=<YOUR_TENCENT_SECRET_KEY>

# 腾讯云COS对象存储
# 获取方式：腾讯云控制台 → 对象存储
TENCENT_COS_BUCKET=<YOUR_BUCKET_NAME>
TENCENT_COS_REGION=ap-shanghai

# ===================================
# 阿里云服务配置
# ===================================
# 阿里云AccessKey
# 获取方式：阿里云控制台 → RAM访问控制 → 用户 → 访问密钥
ALIYUN_ACCESS_KEY_ID=<YOUR_ALIYUN_ACCESS_KEY_ID>
ALIYUN_ACCESS_KEY_SECRET=<YOUR_ALIYUN_ACCESS_KEY_SECRET>

# 阿里云短信服务
# 获取方式：阿里云控制台 → 短信服务 → 签名管理/模板管理
ALIYUN_SMS_TEMPLATE_CODE=SMS_XXXXXX
ALIYUN_SMS_SIGN_NAME=<YOUR_SIGN_NAME>

# ===================================
# 安全密钥（请生成随机字符串）
# ===================================
# 生成方式：在终端运行 openssl rand -hex 32
SESSION_SECRET=<GENERATE_RANDOM_STRING_AT_LEAST_32_CHARS>
JWT_SECRET=<GENERATE_RANDOM_STRING_AT_LEAST_32_CHARS>

# ===================================
# 其他配置（可选）
# ===================================
CORS_ORIGINS=*

# Firebase Admin SDK（如需推送通知）
# FIREBASE_PROJECT_ID=<YOUR_PROJECT_ID>
# FIREBASE_CLIENT_EMAIL=<YOUR_CLIENT_EMAIL>
# FIREBASE_PRIVATE_KEY=<YOUR_PRIVATE_KEY>

# SendCloud邮件服务（如需邮件通知）
# SENDCLOUD_API_USER=<YOUR_API_USER>
# SENDCLOUD_API_KEY=<YOUR_API_KEY>
